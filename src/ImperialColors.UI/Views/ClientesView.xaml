<UserControl x:Class="ImperialColors.UI.Views.ClientesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <ResourceDictionary Source="/Resources/AppTheme.xaml"/>
    </UserControl.Resources>

    <Grid Margin="30,25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel>
                <TextBlock Style="{StaticResource TituloModulo}" Text="Clientes"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Style="{StaticResource BotaoSecundario}" Command="{Binding EditarClienteCommand}"
                        Margin="0,0,10,0" Content="✏  Editar"/>
                <Button Style="{StaticResource BotaoPerigo}" Command="{Binding ExcluirClienteCommand}"
                        Margin="0,0,10,0" Content="🗑  Excluir"/>
                <Button Style="{StaticResource BotaoPrimario}" Command="{Binding NovoClienteCommand}"
                        Content="+ Novo Cliente"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="15">
            <TextBox Style="{StaticResource TextBoxModerno}" Padding="40,8,10,8"
                     Text="{Binding TermoBusca, UpdateSourceTrigger=PropertyChanged}"/>
        </Border>

        <Border Grid.Row="4" Style="{StaticResource Card}" Padding="0">
            <DataGrid Style="{StaticResource DataGridModerno}" ColumnHeaderStyle="{StaticResource DataGridHeaderModerno}"
                      ItemsSource="{Binding Clientes}" SelectedItem="{Binding ClienteSelecionado}"
                      MouseDoubleClick="DataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                    <DataGridTextColumn Header="Telefone" Binding="{Binding Telefone}" Width="130"/>
                    <DataGridTextColumn Header="WhatsApp" Binding="{Binding WhatsApp}" Width="130"/>
                    <DataGridTextColumn Header="E-mail" Binding="{Binding Email}" Width="180"/>
                    <DataGridTextColumn Header="Cidade" Binding="{Binding Cidade}" Width="120"/>
                    <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="60"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
