<UserControl x:Class="ImperialColors.UI.Views.RelatoriosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <ResourceDictionary Source="/Resources/AppTheme.xaml"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="30,25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <TextBlock Style="{StaticResource TituloModulo}" Text="Relatórios"/>
                <TextBlock Text="Gere e exporte relatórios do sistema" FontSize="13"
                           Foreground="{StaticResource CinzaTextoBrush}" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Filtros de período -->
            <Border Grid.Row="2" Style="{StaticResource Card}" Padding="20">
                <StackPanel>
                    <TextBlock Text="Período" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="De:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <DatePicker x:Name="DpInicio" Width="150" Margin="0,0,15,0" SelectedDateFormat="Short"/>
                        <TextBlock Text="Até:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <DatePicker x:Name="DpFim" Width="150" Margin="0,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Cards de relatórios -->
            <UniformGrid Grid.Row="4" Columns="3">
                <!-- Relatório de Vendas PDF -->
                <Border Style="{StaticResource Card}" Margin="0,0,15,15">
                    <StackPanel>
                        <TextBlock Text="📊" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Vendas por Período" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Exportar PDF" FontSize="11" Foreground="{StaticResource CinzaTextoBrush}"
                                   HorizontalAlignment="Center" Margin="0,3,0,15"/>
                        <Button Style="{StaticResource BotaoPrimario}" Content="Gerar PDF"
                                Click="BtnVendasPdf_Click"/>
                    </StackPanel>
                </Border>

                <!-- Relatório de Vendas Excel -->
                <Border Style="{StaticResource Card}" Margin="0,0,15,15">
                    <StackPanel>
                        <TextBlock Text="📈" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Vendas por Período" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Exportar Excel" FontSize="11" Foreground="{StaticResource CinzaTextoBrush}"
                                   HorizontalAlignment="Center" Margin="0,3,0,15"/>
                        <Button Style="{StaticResource BotaoSucesso}" Content="Gerar Excel"
                                Click="BtnVendasExcel_Click"/>
                    </StackPanel>
                </Border>

                <!-- Estoque PDF -->
                <Border Style="{StaticResource Card}" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="📦" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Estoque Completo" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Exportar PDF" FontSize="11" Foreground="{StaticResource CinzaTextoBrush}"
                                   HorizontalAlignment="Center" Margin="0,3,0,15"/>
                        <Button Style="{StaticResource BotaoPrimario}" Content="Gerar PDF"
                                Click="BtnEstoquePdf_Click"/>
                    </StackPanel>
                </Border>

                <!-- Estoque Excel -->
                <Border Style="{StaticResource Card}" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="📋" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Estoque Completo" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Exportar Excel" FontSize="11" Foreground="{StaticResource CinzaTextoBrush}"
                                   HorizontalAlignment="Center" Margin="0,3,0,15"/>
                        <Button Style="{StaticResource BotaoSucesso}" Content="Gerar Excel"
                                Click="BtnEstoqueExcel_Click"/>
                    </StackPanel>
                </Border>

                <!-- Estoque Baixo PDF -->
                <Border Style="{StaticResource Card}" Margin="0,0,15,0">
                    <StackPanel>
                        <TextBlock Text="⚠" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Estoque Baixo" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Produtos abaixo do mínimo" FontSize="11" Foreground="{StaticResource CinzaTextoBrush}"
                                   HorizontalAlignment="Center" Margin="0,3,0,15"/>
                        <Button Style="{StaticResource BotaoPerigo}" Content="Gerar Relatório"
                                Click="BtnEstoqueBaixoPdf_Click"/>
                    </StackPanel>
                </Border>

                <!-- Produtos Sem Estoque -->
                <Border Style="{StaticResource Card}" Margin="0,0,0,0">
                    <StackPanel>
                        <TextBlock Text="🚫" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Sem Estoque" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        <TextBlock Text="Produtos zerados" FontSize="11" Foreground="{StaticResource CinzaTextoBrush}"
                                   HorizontalAlignment="Center" Margin="0,3,0,15"/>
                        <Button Style="{StaticResource BotaoPerigo}" Content="Gerar Relatório"
                                Click="BtnSemEstoquePdf_Click"/>
                    </StackPanel>
                </Border>
            </UniformGrid>
        </Grid>
    </ScrollViewer>
</UserControl>
