<UserControl x:Class="ImperialColors.UI.Views.VendasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:ImperialColors.UI.Converters">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/AppTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <conv:MoedaConverter x:Key="MoedaConverter"/>
            <conv:DataHoraConverter x:Key="DataHoraConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="30,25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel>
                <TextBlock Style="{StaticResource TituloModulo}" Text="Histórico de Vendas"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Style="{StaticResource BotaoSecundario}" Command="{Binding ImprimirCupomCommand}"
                        Margin="0,0,10,0" Content="🧾  Cupom"/>
                <Button Style="{StaticResource BotaoPerigo}" Command="{Binding CancelarVendaCommand}"
                        Margin="0,0,10,0" Content="✕  Cancelar Venda"/>
                <Button Style="{StaticResource BotaoPrimario}" Command="{Binding NovaVendaCommand}"
                        Content="+ Nova Venda (PDV)"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="De:" VerticalAlignment="Center" Margin="0,0,8,0" Style="{StaticResource LabelCampo}"/>
                <DatePicker Grid.Column="1" SelectedDate="{Binding DataInicio}" SelectedDateFormat="Short"/>
                <TextBlock Grid.Column="2" Text="Até:" VerticalAlignment="Center" Margin="15,0,8,0" Style="{StaticResource LabelCampo}"/>
                <DatePicker Grid.Column="3" SelectedDate="{Binding DataFim}" SelectedDateFormat="Short"/>
                <TextBox Grid.Column="4" Style="{StaticResource TextBoxFiltro}" Margin="15,0,15,0"
                         Text="{Binding TermoBusca, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Grid.Column="5" Style="{StaticResource BotaoPrimario}" Command="{Binding FiltrarCommand}"
                        Content="🔍  Filtrar" Padding="15,8"/>
            </Grid>
        </Border>

        <Border Grid.Row="4" Style="{StaticResource Card}" Padding="0">
            <DataGrid Style="{StaticResource DataGridModerno}" ColumnHeaderStyle="{StaticResource DataGridHeaderModerno}"
                      ItemsSource="{Binding Vendas}" SelectedItem="{Binding VendaSelecionada}"
                      MouseDoubleClick="DataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nº Venda" Binding="{Binding NumeroVenda}" Width="130"/>
                    <DataGridTextColumn Header="Data" Binding="{Binding DataVenda, Converter={StaticResource DataHoraConverter}}" Width="150"/>
                    <DataGridTextColumn Header="Cliente" Binding="{Binding ClienteNome}" Width="*"/>
                    <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, Converter={StaticResource MoedaConverter}}" Width="100"/>
                    <DataGridTextColumn Header="Desconto" Binding="{Binding Desconto, Converter={StaticResource MoedaConverter}}" Width="100"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding Total, Converter={StaticResource MoedaConverter}}" Width="110"/>
                    <DataGridTemplateColumn Header="Status" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border HorizontalAlignment="Center" Padding="8,3" CornerRadius="4">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#D4EDDA"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding StatusDescricao}" Value="Cancelada">
                                                    <Setter Property="Background" Value="#F8D7DA"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding StatusDescricao}" Value="Aberta">
                                                    <Setter Property="Background" Value="#FFF3CD"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding StatusDescricao}" FontSize="11" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#155724"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding StatusDescricao}" Value="Cancelada">
                                                        <Setter Property="Foreground" Value="#721C24"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding StatusDescricao}" Value="Aberta">
                                                        <Setter Property="Foreground" Value="#856404"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="5" Style="{StaticResource Card}" Padding="12,8" Margin="0,10,0,0">
            <Grid>
                <TextBlock VerticalAlignment="Center" Foreground="{StaticResource CinzaTextoBrush}"
                           Text="{Binding InfoPaginacao}"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource BotaoSecundario}" Content="◀ Anterior"
                            Command="{Binding PaginaAnteriorCommand}" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource BotaoSecundario}" Content="Próxima ▶"
                            Command="{Binding PaginaProximaCommand}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
